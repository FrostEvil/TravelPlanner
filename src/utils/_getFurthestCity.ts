// import { GeocodedData } from "@/types/type";

// function getFurthestCity(showTrawel: GeocodedData[]): GeocodedData {
//   showTrawel.sort((a, b) => {
//     return b.distanceToStartingCity! - a.distanceToStartingCity!;
//   });
//   return showTrawel[0];
// }

// export default getFurthestCity;
// let result = brokers.reduce((unique, object) => {
//   if (!unique.some((obj) => obj.name === object.name))
//     unique.push(object);
//   return unique;
// }, []);
